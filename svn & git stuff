How to access svn and git stuff

svn checkout --depth empty http://scm.iqhealth-emr.cerner.corp/svn/sandbox/ 

svn add <thing> 

to get something ready to be added

svn delete <thing>

to delete something

svn status 

for looking at stuff

svn commit -m ‘message’

to commit to the repo


git status

git add

git branch

git commit -m ‘asdf’

git push origin branch

